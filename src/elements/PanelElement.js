import React from 'react';

import { View, Text } from 'react-native';

const PanelElement = props => {

    return (
        <View>
            <Text>PanelElement still under construction...</Text>
        </View>
    );
};

export default PanelElement;