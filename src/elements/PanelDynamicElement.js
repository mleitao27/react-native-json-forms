import React from 'react';

import { View, Text } from 'react-native';

const PanelDynamicElement = props => {

    return (
        <View>
            <Text>PanelDynamicElement still under construction...</Text>
        </View>
    );
};

export default PanelDynamicElement;